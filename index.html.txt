<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nexor Clan - Recrutamento Call of Duty</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'nexor-red': '#ff0000',
                        'nexor-dark': '#0a0a0a',
                        'nexor-gray': '#1a1a1a',
                        'nexor-light': '#2a2a2a',
                    },
                    fontFamily: {
                        'orbitron': ['Orbitron', 'sans-serif'],
                        'rajdhani': ['Rajdhani', 'sans-serif'],
                    },
                    animation: {
                        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
                        'glitch': 'glitch 2s linear infinite',
                        'flicker': 'flicker 5s linear infinite',
                    },
                    keyframes: {
                        glitch: {
                            '0%': { transform: 'translate(0)' },
                            '20%': { transform: 'translate(-2px, 2px)' },
                            '40%': { transform: 'translate(-2px, -2px)' },
                            '60%': { transform: 'translate(2px, 2px)' },
                            '80%': { transform: 'translate(2px, -2px)' },
                            '100%': { transform: 'translate(0)' },
                        },
                        flicker: {
                            '0%': { opacity: 0.9 },
                            '3%': { opacity: 0.8 },
                            '6%': { opacity: 0.9 },
                            '7%': { opacity: 0.1 },
                            '8%': { opacity: 0.9 },
                            '9%': { opacity: 0.2 },
                            '10%': { opacity: 0.9 },
                            '89%': { opacity: 0.9 },
                            '90%': { opacity: 0.4 },
                            '100%': { opacity: 0.9 },
                        }
                    }
                }
            }
        }
    </script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@300;500;700&display=swap');
        
        body {
            font-family: 'Rajdhani', sans-serif;
            background-color: #0a0a0a;
            color: #ffffff;
            overflow-x: hidden;
        }
        
        .tech-border {
            position: relative;
            border: 1px solid rgba(255, 0, 0, 0.3);
        }
        
        .tech-border::before, .tech-border::after {
            content: '';
            position: absolute;
            width: 20px;
            height: 20px;
            border-color: #ff0000;
            border-style: solid;
            transition: all 0.5s;
        }
        
        .tech-border::before {
            top: 0;
            left: 0;
            border-width: 2px 0 0 2px;
        }
        
        .tech-border::after {
            bottom: 0;
            right: 0;
            border-width: 0 2px 2px 0;
        }
        
        .tech-border:hover::before, .tech-border:hover::after {
            width: 100%;
            height: 100%;
            border-color: #ff0000;
        }
        
        .scanline {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, 
                rgba(255, 0, 0, 0.03) 0%, 
                rgba(255, 0, 0, 0.03) 50%, 
                rgba(0, 0, 0, 0.2) 51%, 
                rgba(0, 0, 0, 0.2) 100%);
            background-size: 100% 5px;
            pointer-events: none;
            animation: scanline 8s linear infinite;
        }
        
        @keyframes scanline {
            0% { transform: translateY(-100%); }
            100% { transform: translateY(100%); }
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #ff0000 0%, #800000 100%);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            border: none;
            font-weight: bold;
            letter-spacing: 1px;
            text-transform: uppercase;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 0, 0, 0.6);
        }
        
        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }
        
        .btn-primary:hover::before {
            left: 100%;
        }
        
        .form-input {
            background: rgba(30, 30, 30, 0.8);
            border: 1px solid rgba(255, 0, 0, 0.3);
            color: white;
            transition: all 0.3s;
        }
        
        .form-input:focus {
            box-shadow: 0 0 0 3px rgba(255, 0, 0, 0.3);
            border-color: #ff0000;
            background: rgba(40, 40, 40, 0.8);
        }
        
        .notification {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1000;
            transform: translateX(150%);
            transition: transform 0.5s ease;
        }
        
        .notification.show {
            transform: translateX(0);
        }
        
        .pixel-corners {
            clip-path: 
                polygon(
                    0% 5px, 5px 5px, 5px 0%, calc(100% - 5px) 0%, 
                    calc(100% - 5px) 5px, 100% 5px, 100% calc(100% - 5px), 
                    calc(100% - 5px) calc(100% - 5px), calc(100% - 5px) 100%, 
                    5px 100%, 5px calc(100% - 5px), 0% calc(100% - 5px)
                );
        }
        
        .profile-card {
            transition: all 0.3s ease;
            background: rgba(20, 20, 20, 0.8);
            border: 1px solid rgba(255, 0, 0, 0.3);
            box-shadow: 0 0 15px rgba(255, 0, 0, 0.2);
        }
        
        .profile-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(255, 0, 0, 0.5);
            border-color: rgba(255, 0, 0, 0.7);
        }
        
        .character-builder {
            background: rgba(30, 30, 30, 0.8);
            border: 1px dashed rgba(255, 0, 0, 0.5);
        }
        
        .character-builder:hover {
            background: rgba(40, 40, 40, 0.8);
            border: 1px solid rgba(255, 0, 0, 0.7);
        }
        
        .hexagon {
            clip-path: polygon(50% 0%, 100% 25%, 100% 75%, 50% 100%, 0% 75%, 0% 25%);
        }
        
        .status-online {
            box-shadow: 0 0 10px rgba(0, 255, 0, 0.7);
        }
        
        .status-offline {
            box-shadow: 0 0 10px rgba(255, 0, 0, 0.3);
        }
    </style>
</head>
<body class="bg-nexor-dark text-white">
    <!-- Navigation -->
    <nav class="bg-black bg-opacity-90 fixed w-full z-50 border-b border-red-900">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <div class="flex items-center">
                    <div class="flex-shrink-0">
                        <span class="text-2xl font-orbitron text-nexor-red">NEXOR</span>
                    </div>
                    <div class="hidden md:block">
                        <div class="ml-10 flex items-baseline space-x-4">
                            <a href="#home" class="px-3 py-2 rounded-md text-sm font-medium text-white hover:bg-red-900 hover:text-white transition-all">INÍCIO</a>
                            <a href="#about" class="px-3 py-2 rounded-md text-sm font-medium text-gray-300 hover:text-white hover:bg-red-900 transition-all">SOBRE</a>
                            <a href="#members" class="px-3 py-2 rounded-md text-sm font-medium text-gray-300 hover:text-white hover:bg-red-900 transition-all">MEMBROS</a>
                            <a href="#join" class="px-3 py-2 rounded-md text-sm font-medium text-gray-300 hover:text-white hover:bg-red-900 transition-all">RECRUTAMENTO</a>
                            <a href="#contact" class="px-3 py-2 rounded-md text-sm font-medium text-gray-300 hover:text-white hover:bg-red-900 transition-all">CONTATO</a>
                        </div>
                    </div>
                </div>
                <div class="hidden md:block">
                    <div class="ml-4 flex items-center md:ml-6">
                        <a href="#join" class="btn-primary px-4 py-2 rounded-md text-sm font-bold uppercase animate-pulse-slow">JUNTE-SE AGORA</a>
                    </div>
                </div>
                <div class="-mr-2 flex md:hidden">
                    <button type="button" id="mobile-menu-button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-red-900 focus:outline-none">
                        <span class="sr-only">Abrir menu</span>
                        <i class="fas fa-bars"></i>
                    </button>
                </div>
            </div>
        </div>
        
        <!-- Mobile menu -->
        <div class="hidden md:hidden" id="mobile-menu">
            <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3 bg-black border-b border-red-900">
                <a href="#home" class="block px-3 py-2 rounded-md text-base font-medium text-white hover:bg-red-900">INÍCIO</a>
                <a href="#about" class="block px-3 py-2 rounded-md text-base font-medium text-gray-300 hover:text-white hover:bg-red-900">SOBRE</a>
                <a href="#members" class="block px-3 py-2 rounded-md text-base font-medium text-gray-300 hover:text-white hover:bg-red-900">MEMBROS</a>
                <a href="#join" class="block px-3 py-2 rounded-md text-base font-medium text-gray-300 hover:text-white hover:bg-red-900">RECRUTAMENTO</a>
                <a href="#contact" class="block px-3 py-2 rounded-md text-base font-medium text-gray-300 hover:text-white hover:bg-red-900">CONTATO</a>
                <a href="#join" class="block px-3 py-2 rounded-md text-base font-bold uppercase btn-primary text-center animate-pulse-slow">JUNTE-SE AGORA</a>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section id="home" class="relative pt-32 pb-20 px-4 sm:px-6 lg:px-8 bg-gradient-to-b from-black to-nexor-gray">
        <div class="scanline"></div>
        <div class="max-w-4xl mx-auto relative z-10">
            <h1 class="text-4xl md:text-6xl font-orbitron mb-6 text-white text-center animate-glitch">
                NEXOR CLAN
            </h1>
            <p class="text-xl md:text-2xl mb-8 text-gray-200 font-medium text-center">
                O FUTURO DA COMPETIÇÃO EM CALL OF DUTY
            </p>
            <div class="flex flex-col sm:flex-row justify-center gap-4">
                <a href="#join" class="btn-primary px-8 py-4 rounded-md text-lg font-bold uppercase animate-pulse-slow">RECRUTAMENTO</a>
                <a href="#members" class="bg-nexor-gray border border-nexor-red px-8 py-4 rounded-md text-lg font-bold uppercase hover:bg-nexor-dark transition-all">VER MEMBROS</a>
            </div>
        </div>
        
        <!-- Animated grid background -->
        <div class="absolute inset-0 overflow-hidden z-0 opacity-20">
            <div class="absolute inset-0 bg-grid-red-500/20" style="background-image: url('data:image/svg+xml;base64,')"></div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="py-20 px-4 sm:px-6 lg:px-8 bg-nexor-dark">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-3xl md:text-4xl font-orbitron mb-12 text-center text-nexor-red">// SOBRE O NEXOR</h2>
            
            <div class="grid md:grid-cols-3 gap-8">
                <div class="tech-border p-6 rounded-lg">
                    <div class="text-nexor-red text-4xl mb-4">
                        <i class="fas fa-crosshairs"></i>
                    </div>
                    <h3 class="text-xl font-bold mb-3 text-white font-orbitron">MISSÃO</h3>
                    <p class="text-gray-300">
                        Construir o clã mais forte e unido de Call of Duty, onde cada membro é essencial para nossa jornada rumo ao topo.
                    </p>
                </div>
                
                <div class="tech-border p-6 rounded-lg">
                    <div class="text-nexor-red text-4xl mb-4">
                        <i class="fas fa-users"></i>
                    </div>
                    <h3 class="text-xl font-bold mb-3 text-white font-orbitron">COMUNIDADE</h3>
                    <p class="text-gray-300">
                        Um grupo de jogadores apaixonados que valorizam o trabalho em equipe, respeito e melhoria constante.
                    </p>
                </div>
                
                <div class="tech-border p-6 rounded-lg">
                    <div class="text-nexor-red text-4xl mb-4">
                        <i class="fas fa-trophy"></i>
                    </div>
                    <h3 class="text-xl font-bold mb-3 text-white font-orbitron">FUTURO</h3>
                    <p class="text-gray-300">
                        Estamos construindo nossa história. Seja parte do início de uma lenda nos torneios competitivos.
                    </p>
                </div>
            </div>
            
            <div class="mt-16 tech-border p-8 rounded-lg">
                <h3 class="text-2xl font-bold mb-4 text-center text-nexor-red font-orbitron">// NOSSA HISTÓRIA</h3>
                <p class="text-gray-300 text-center max-w-4xl mx-auto">
                    Fundado em 2025 por Gabriel, o Nexor nasceu da vontade de criar um clã diferente - onde a habilidade se junta ao respeito e à diversão. Somos novos, mas nossa ambição é grande. Estamos recrutando jogadores que queiram crescer conosco e marcar nosso nome na cena competitiva.
                </p>
                
                <!-- Founder -->
                <div class="grid grid-cols-1 gap-8 mt-12">
                    <div class="tech-border p-6 rounded-lg flex items-center">
                        <div class="w-20 h-20 rounded-full bg-nexor-red flex items-center justify-center text-2xl mr-4">
                            <i class="fas fa-crown"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-white font-orbitron">GABRIEL</h4>
                            <p class="text-gray-400">Fundador e Líder</p>
                            <p class="text-sm text-nexor-red">"Construímos o Nexor para ser diferente."</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Members Section -->
    <section id="members" class="py-20 px-4 sm:px-6 lg:px-8 bg-nexor-gray">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-3xl md:text-4xl font-orbitron mb-12 text-center text-nexor-red">// MEMBROS DO CLÃ</h2>
            
            <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6" id="membersGrid">
                <!-- Members will be added dynamically -->
                <div class="profile-card p-4 rounded-lg text-center">
                    <div class="w-24 h-24 rounded-full bg-nexor-red mx-auto mb-3 flex items-center justify-center text-3xl status-online">
                        <i class="fas fa-crown"></i>
                    </div>
                    <h3 class="font-bold text-white">GABRIEL</h3>
                    <p class="text-sm text-nexor-red">Líder</p>
                    <p class="text-xs text-gray-400 mt-2">Online agora</p>
                </div>
            </div>
            
            <!-- Character Builder -->
            <div id="characterBuilder" class="hidden tech-border p-8 rounded-lg mt-12">
                <h3 class="text-2xl font-bold mb-6 text-nexor-red text-center font-orbitron">// CRIAR PERSONAGEM</h3>
                
                <div class="grid md:grid-cols-2 gap-8">
                    <div>
                        <div class="mb-6">
                            <label for="charName" class="block text-sm font-medium text-gray-300 mb-1">NOME DO PERSONAGEM *</label>
                            <input type="text" id="charName" class="w-full px-4 py-3 rounded-md form-input">
                        </div>
                        
                        <div class="mb-6">
                            <label class="block text-sm font-medium text-gray-300 mb-1">CLASSE *</label>
                            <div class="grid grid-cols-2 gap-4">
                                <button type="button" class="character-builder p-4 rounded-md text-center" data-class="assault">
                                    <i class="fas fa-fire text-2xl mb-2 text-nexor-red"></i>
                                    <p class="font-bold">Assaltante</p>
                                </button>
                                <button type="button" class="character-builder p-4 rounded-md text-center" data-class="support">
                                    <i class="fas fa-medkit text-2xl mb-2 text-nexor-red"></i>
                                    <p class="font-bold">Suporte</p>
                                </button>
                                <button type="button" class="character-builder p-4 rounded-md text-center" data-class="sniper">
                                    <i class="fas fa-crosshairs text-2xl mb-2 text-nexor-red"></i>
                                    <p class="font-bold">Sniper</p>
                                </button>
                                <button type="button" class="character-builder p-4 rounded-md text-center" data-class="tactical">
                                    <i class="fas fa-bullseye text-2xl mb-2 text-nexor-red"></i>
                                    <p class="font-bold">Tático</p>
                                </button>
                            </div>
                        </div>
                        
                        <div class="mb-6">
                            <label for="charWeapon" class="block text-sm font-medium text-gray-300 mb-1">ARMA PRINCIPAL *</label>
                            <select id="charWeapon" class="w-full px-4 py-3 rounded-md form-input">
                                <option value="">SELECIONE</option>
                                <option value="m4">M4A1</option>
                                <option value="ak47">AK-47</option>
                                <option value="mp5">MP5</option>
                                <option value="ax50">AX-50</option>
                                <option value="kar98k">Kar98k</option>
                            </select>
                        </div>
                    </div>
                    
                    <div>
                        <div class="mb-6">
                            <label class="block text-sm font-medium text-gray-300 mb-1">APARÊNCIA</label>
                            <div class="flex justify-center mb-4">
                                <div class="hexagon w-32 h-32 bg-nexor-light flex items-center justify-center">
                                    <i class="fas fa-user text-4xl text-gray-400" id="charPreview"></i>
                                </div>
                            </div>
                            <div class="grid grid-cols-4 gap-2">
                                <button type="button" class="p-2 bg-nexor-light rounded hover:bg-nexor-red" data-skin="male1">
                                    <i class="fas fa-male text-nexor-red"></i>
                                </button>
                                <button type="button" class="p-2 bg-nexor-light rounded hover:bg-nexor-red" data-skin="male2">
                                    <i class="fas fa-male text-nexor-red"></i>
                                </button>
                                <button type="button" class="p-2 bg-nexor-light rounded hover:bg-nexor-red" data-skin="female1">
                                    <i class="fas fa-female text-nexor-red"></i>
                                </button>
                                <button type="button" class="p-2 bg-nexor-light rounded hover:bg-nexor-red" data-skin="female2">
                                    <i class="fas fa-female text-nexor-red"></i>
                                </button>
                            </div>
                        </div>
                        
                        <div class="mb-6">
                            <label for="charBio" class="block text-sm font-medium text-gray-300 mb-1">BIOGRAFIA</label>
                            <textarea id="charBio" rows="3" class="w-full px-4 py-3 rounded-md form-input" placeholder="Descreva seu personagem..."></textarea>
                        </div>
                    </div>
                </div>
                
                <div class="flex justify-center gap-4 mt-6">
                    <button type="button" id="saveCharacter" class="btn-primary px-8 py-3 rounded-md font-bold uppercase">
                        <i class="fas fa-save mr-2"></i> SALVAR
                    </button>
                    <button type="button" id="cancelCharacter" class="bg-nexor-gray border border-nexor-red px-8 py-3 rounded-md font-bold uppercase hover:bg-nexor-dark">
                        <i class="fas fa-times mr-2"></i> CANCELAR
                    </button>
                </div>
            </div>
            
            <div class="text-center mt-12">
                <p class="text-gray-400 mb-4">SE TORNE O PRÓXIMO MEMBRO DO NEXOR</p>
                <a href="#join" class="btn-primary inline-flex items-center px-8 py-3 rounded-md text-lg font-bold uppercase animate-pulse-slow">
                    <i class="fas fa-user-plus mr-2"></i> JUNTE-SE A NÓS
                </a>
                <button id="createCharacterBtn" class="hidden bg-nexor-gray border border-nexor-red px-8 py-3 rounded-md text-lg font-bold uppercase hover:bg-nexor-dark ml-4">
                    <i class="fas fa-user-edit mr-2"></i> CRIAR PERSONAGEM
                </button>
            </div>
        </div>
    </section>

    <!-- Join Section -->
    <section id="join" class="py-20 px-4 sm:px-6 lg:px-8 bg-nexor-dark">
        <div class="max-w-4xl mx-auto">
            <h2 class="text-3xl md:text-4xl font-orbitron mb-12 text-center text-nexor-red">// RECRUTAMENTO</h2>
            
            <div class="tech-border p-8 rounded-lg relative overflow-hidden">
                <div class="absolute top-0 right-0 text-nexor-red text-xs font-orbitron p-2 bg-black border-b border-l border-red-900">
                    FORMULÁRIO DE INSCRIÇÃO
                </div>
                
                <form id="recruitmentForm" class="space-y-6">
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="name" class="block text-sm font-medium text-gray-300 mb-1">NOME COMPLETO *</label>
                            <input type="text" id="name" name="name" required class="w-full px-4 py-3 rounded-md form-input">
                        </div>
                        <div>
                            <label for="age" class="block text-sm font-medium text-gray-300 mb-1">IDADE *</label>
                            <input type="number" id="age" name="age" required class="w-full px-4 py-3 rounded-md form-input">
                        </div>
                    </div>
                    
                    <div>
                        <label for="email" class="block text-sm font-medium text-gray-300 mb-1">EMAIL *</label>
                        <input type="email" id="email" name="email" required class="w-full px-4 py-3 rounded-md form-input">
                    </div>
                    
                    <div>
                        <label for="cod-id" class="block text-sm font-medium text-gray-300 mb-1">ID DO CALL OF DUTY *</label>
                        <input type="text" id="cod-id" name="cod-id" required class="w-full px-4 py-3 rounded-md form-input" placeholder="Ex: Player#1234567">
                    </div>
                    
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="platform" class="block text-sm font-medium text-gray-300 mb-1">PLATAFORMA *</label>
                            <select id="platform" name="platform" required class="w-full px-4 py-3 rounded-md form-input">
                                <option value="">SELECIONE</option>
                                <option value="pc">PC</option>
                                <option value="ps5">PLAYSTATION 5</option>
                                <option value="xbox">XBOX SERIES X/S</option>
                            </select>
                        </div>
                        <div>
                            <label for="region" class="block text-sm font-medium text-gray-300 mb-1">REGIÃO *</label>
                            <select id="region" name="region" required class="w-full px-4 py-3 rounded-md form-input">
                                <option value="">SELECIONE</option>
                                <option value="br">BRASIL</option>
                                <option value="na">AMÉRICA DO NORTE</option>
                                <option value="eu">EUROPA</option>
                            </select>
                        </div>
                    </div>
                    
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="kd" class="block text-sm font-medium text-gray-300 mb-1">KD RATIO *</label>
                            <input type="number" step="0.01" id="kd" name="kd" required class="w-full px-4 py-3 rounded-md form-input">
                        </div>
                        <div>
                            <label for="playstyle" class="block text-sm font-medium text-gray-300 mb-1">ESTILO DE JOGO *</label>
                            <select id="playstyle" name="playstyle" required class="w-full px-4 py-3 rounded-md form-input">
                                <option value="">SELECIONE</option>
                                <option value="aggressive">AGRESSIVO</option>
                                <option value="strategic">ESTRATÉGICO</option>
                                <option value="support">SUPORTE</option>
                                <option value="sniper">SNIPER</option>
                            </select>
                        </div>
                    </div>
                    
                    <div>
                        <label for="experience" class="block text-sm font-medium text-gray-300 mb-1">EXPERIÊNCIA *</label>
                        <textarea id="experience" name="experience" rows="3" required class="w-full px-4 py-3 rounded-md form-input" placeholder="Quanto tempo joga COD? Já participou de outros clãs?"></textarea>
                    </div>
                    
                    <div>
                        <label for="why-join" class="block text-sm font-medium text-gray-300 mb-1">POR QUE QUER ENTRAR NO NEXOR? *</label>
                        <textarea id="why-join" name="why-join" rows="3" required class="w-full px-4 py-3 rounded-md form-input" placeholder="O que você pode contribuir para o clã?"></textarea>
                    </div>
                    
                    <div class="flex items-start">
                        <input id="terms" name="terms" type="checkbox" required class="h-4 w-4 rounded form-input mt-1">
                        <label for="terms" class="ml-2 block text-sm text-gray-300">
                            EU CONCORDO COM OS <a href="#" class="text-nexor-red hover:underline">TERMOS E CONDIÇÕES</a> E ACEITO QUE MEUS DADOS SERÃO USADOS PARA AVALIAÇÃO
                        </label>
                    </div>
                    
                    <div class="pt-4">
                        <button type="submit" class="btn-primary w-full py-4 rounded-md text-lg font-bold uppercase animate-pulse-slow">
                            <i class="fas fa-paper-plane mr-2"></i> ENVIAR INSCRIÇÃO
                        </button>
                    </div>
                </form>
                
                <div id="successMessage" class="hidden mt-6 p-4 bg-nexor-red bg-opacity-20 text-white rounded-md border border-nexor-red">
                    <div class="flex items-center">
                        <i class="fas fa-check-circle text-2xl mr-3 text-green-400"></i>
                        <div>
                            <p class="font-bold">INSCRIÇÃO ENVIADA COM SUCESSO!</p>
                            <p class="text-sm">Sua solicitação foi enviada para os líderes do clã. Você receberá uma notificação quando sua aplicação for revisada.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="py-20 px-4 sm:px-6 lg:px-8 bg-nexor-gray">
        <div class="max-w-7xl mx-auto">
            <h2 class="text-3xl md:text-4xl font-orbitron mb-12 text-center text-nexor-red">// CONTATO</h2>
            
            <div class="tech-border p-8 rounded-lg max-w-3xl mx-auto">
                <h3 class="text-2xl font-bold mb-6 text-nexor-red text-center font-orbitron">// ENTRE EM CONTATO</h3>
                
                <div class="space-y-6">
                    <div class="flex flex-col items-center text-center">
                        <div class="text-nexor-red text-4xl mb-3">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <div>
                            <h4 class="font-bold text-white mb-1 font-orbitron">EMAIL OFICIAL</h4>
                            <a href="mailto:gabrielsandys@colegiomieza.com.br" class="text-gray-400 hover:text-nexor-red text-lg">gabrielsandys@colegiomieza.com.br</a>
                        </div>
                    </div>
                    
                    <div class="mt-8 text-center">
                        <p class="text-gray-400 mb-4">PARA ASSUNTOS DE RECRUTAMENTO, USE O FORMULÁRIO ACIMA</p>
                        <a href="#join" class="btn-primary inline-flex items-center px-8 py-3 rounded-md text-lg font-bold uppercase animate-pulse-slow">
                            <i class="fas fa-arrow-up mr-2"></i> IR PARA RECRUTAMENTO
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-black py-12 px-4 sm:px-6 lg:px-8 border-t border-red-900">
        <div class="max-w-7xl mx-auto">
            <div class="flex flex-col md:flex-row justify-between items-center">
                <div class="mb-6 md:mb-0">
                    <span class="text-2xl font-orbitron text-nexor-red">NEXOR</span>
                    <p class="text-gray-600 mt-2 text-sm">CLÃ DE CALL OF DUTY</p>
                </div>
                
                <div class="flex flex-col items-center md:items-end">
                    <p class="text-gray-600 text-sm mb-2">© 2025 NEXOR CLAN. TODOS OS DIREITOS RESERVADOS.</p>
                    <p class="text-gray-600 text-xs">CALL OF DUTY É UMA MARCA REGISTRADA DA ACTIVISION PUBLISHING, INC.</p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Admin Notification -->
    <div class="notification bg-black border border-nexor-red p-4 rounded-md max-w-sm" id="adminNotification">
        <div class="flex justify-between items-start">
            <div class="flex">
                <div class="text-nexor-red text-xl mr-3">
                    <i class="fas fa-bell"></i>
                </div>
                <div>
                    <h4 class="font-bold text-white">NOVA INSCRIÇÃO</h4>
                    <p class="text-sm text-gray-400" id="notificationText">Novo jogador quer entrar no clã.</p>
                </div>
            </div>
            <button class="text-gray-400 hover:text-white" id="closeNotification">
                <i class="fas fa-times"></i>
            </button>
        </div>
        <div class="flex justify-end gap-2 mt-3">
            <button class="px-3 py-1 bg-green-600 text-xs rounded hover:bg-green-700" id="acceptBtn">ACEITAR</button>
            <button class="px-3 py-1 bg-red-600 text-xs rounded hover:bg-red-700" id="rejectBtn">RECUSAR</button>
        </div>
    </div>

    <script>
        // Mobile menu toggle
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');
        
        mobileMenuButton.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });
        
        // Form submission
        const recruitmentForm = document.getElementById('recruitmentForm');
        const successMessage = document.getElementById('successMessage');
        const adminNotification = document.getElementById('adminNotification');
        const notificationText = document.getElementById('notificationText');
        const closeNotification = document.getElementById('closeNotification');
        const acceptBtn = document.getElementById('acceptBtn');
        const rejectBtn = document.getElementById('rejectBtn');
        const membersGrid = document.getElementById('membersGrid');
        const createCharacterBtn = document.getElementById('createCharacterBtn');
        const characterBuilder = document.getElementById('characterBuilder');
        const saveCharacter = document.getElementById('saveCharacter');
        const cancelCharacter = document.getElementById('cancelCharacter');
        const charPreview = document.getElementById('charPreview');
        
        // Character builder variables
        let selectedClass = '';
        let selectedSkin = 'male1';
        
        // Sample member data (only Gabriel initially)
        const initialMembers = [
            { 
                name: "GABRIEL", 
                role: "Líder", 
                status: "Online agora",
                online: true,
                isLeader: true
            }
        ];
        
        // Track accepted members
        let acceptedMembers = [];
        
        // Initialize members grid
        function initializeMembers() {
            membersGrid.innerHTML = '';
            
            // Add initial members
            initialMembers.forEach(member => {
                const memberElement = document.createElement('div');
                memberElement.className = 'profile-card p-4 rounded-lg text-center';
                memberElement.innerHTML = `
                    <div class="w-24 h-24 rounded-full bg-nexor-red mx-auto mb-3 flex items-center justify-center text-3xl ${member.online ? 'status-online' : 'status-offline'}">
                        ${member.isLeader ? '<i class="fas fa-crown"></i>' : '<i class="fas fa-user"></i>'}
                    </div>
                    <h3 class="font-bold text-white">${member.name}</h3>
                    <p class="text-sm text-nexor-red">${member.role}</p>
                    <p class="text-xs text-gray-400 mt-2">${member.status}</p>
                `;
                membersGrid.appendChild(memberElement);
            });
            
            // Add accepted members
            acceptedMembers.forEach(member => {
                const memberElement = document.createElement('div');
                memberElement.className = 'profile-card p-4 rounded-lg text-center';
                memberElement.innerHTML = `
                    <div class="w-24 h-24 rounded-full bg-nexor-red mx-auto mb-3 flex items-center justify-center text-3xl status-online">
                        <i class="fas fa-user"></i>
                    </div>
                    <h3 class="font-bold text-white">${member.name}</h3>
                    <p class="text-sm text-nexor-red">${member.role || 'Recruta'}</p>
                    <p class="text-xs text-gray-400 mt-2">Online agora</p>
                `;
                membersGrid.appendChild(memberElement);
            });
        }
        
        // Initialize the page
        initializeMembers();
        
        recruitmentForm.addEventListener('submit', (e) => {
            e.preventDefault();
            
            // Get form data
            const formData = new FormData(recruitmentForm);
            const playerName = formData.get('name');
            const codId = formData.get('cod-id');
            const platform = formData.get('platform');
            
            // Simulate form processing
            recruitmentForm.classList.add('opacity-50');
            recruitmentForm.querySelector('button').innerHTML = '<i class="fas fa-spinner fa-spin mr-2"></i> ENVIANDO...';
            
            setTimeout(() => {
                // Show success message
                recruitmentForm.reset();
                recruitmentForm.classList.remove('opacity-50');
                recruitmentForm.querySelector('button').innerHTML = '<i class="fas fa-paper-plane mr-2"></i> ENVIAR INSCRIÇÃO';
                successMessage.classList.remove('hidden');
                
                // Show admin notification
                notificationText.textContent = `${playerName} (${codId} - ${platform.toUpperCase()}) quer entrar no clã.`;
                adminNotification.classList.add('show');
                
                // Store applicant data
                localStorage.setItem('applicantName', playerName);
                localStorage.setItem('applicantCodId', codId);
                
                // Scroll to success message
                successMessage.scrollIntoView({ behavior: 'smooth' });
            }, 1500);
        });
        
        // Close notification
        closeNotification.addEventListener('click', () => {
            adminNotification.classList.remove('show');
        });
        
        // Accept button
        acceptBtn.addEventListener('click', () => {
            // Get the player name from notification
            const playerInfo = notificationText.textContent.split(' quer entrar no clã.')[0];
            const playerName = playerInfo.split(' (')[0];
            const codId = localStorage.getItem('applicantCodId');
            
            // Add new member to accepted members
            acceptedMembers.push({
                name: playerName.toUpperCase(),
                codId: codId,
                role: 'Recruta',
                online: true
            });
            
            // Save to localStorage
            localStorage.setItem('acceptedMembers', JSON.stringify(acceptedMembers));
            
            // Update members grid
            initializeMembers();
            
            // Show create character button
            createCharacterBtn.classList.remove('hidden');
            
            // Show acceptance message
            notificationText.textContent = `${playerName} foi aceito no clã!`;
            acceptBtn.classList.add('hidden');
            rejectBtn.classList.add('hidden');
            
            // Hide notification after 3 seconds
            setTimeout(() => {
                adminNotification.classList.remove('show');
                // Reset notification for next time
                setTimeout(() => {
                    notificationText.textContent = 'Novo jogador quer entrar no clã.';
                    acceptBtn.classList.remove('hidden');
                    rejectBtn.classList.remove('hidden');
                }, 500);
            }, 3000);
        });
        
        // Reject button
        rejectBtn.addEventListener('click', () => {
            const playerInfo = notificationText.textContent.split(' quer entrar no clã.')[0];
            const playerName = playerInfo.split(' (')[0];
            
            // Show rejection message
            notificationText.textContent = `${playerName} foi recusado.`;
            acceptBtn.classList.add('hidden');
            rejectBtn.classList.add('hidden');
            
            // Hide notification after 3 seconds
            setTimeout(() => {
                adminNotification.classList.remove('show');
                // Reset notification for next time
                setTimeout(() => {
                    notificationText.textContent = 'Novo jogador quer entrar no clã.';
                    acceptBtn.classList.remove('hidden');
                    rejectBtn.classList.remove('hidden');
                }, 500);
            }, 3000);
        });
        
        // Create character button
        createCharacterBtn.addEventListener('click', () => {
            characterBuilder.classList.remove('hidden');
            window.scrollTo({
                top: characterBuilder.offsetTop - 100,
                behavior: 'smooth'
            });
        });
        
        // Cancel character creation
        cancelCharacter.addEventListener('click', () => {
            characterBuilder.classList.add('hidden');
        });
        
        // Class selection
        document.querySelectorAll('[data-class]').forEach(button => {
            button.addEventListener('click', function() {
                selectedClass = this.getAttribute('data-class');
                // Update preview icon based on class
                switch(selectedClass) {
                    case 'assault':
                        charPreview.className = 'fas fa-fire text-4xl text-nexor-red';
                        break;
                    case 'support':
                        charPreview.className = 'fas fa-medkit text-4xl text-nexor-red';
                        break;
                    case 'sniper':
                        charPreview.className = 'fas fa-crosshairs text-4xl text-nexor-red';
                        break;
                    case 'tactical':
                        charPreview.className = 'fas fa-bullseye text-4xl text-nexor-red';
                        break;
                }
            });
        });
        
        // Skin selection
        document.querySelectorAll('[data-skin]').forEach(button => {
            button.addEventListener('click', function() {
                selectedSkin = this.getAttribute('data-skin');
                // Update preview based on skin (simplified with icons)
                if (selectedSkin.includes('male')) {
                    charPreview.className = charPreview.className.replace('female', 'male');
                } else {
                    charPreview.className = charPreview.className.replace('male', 'female');
                }
            });
        });
        
        // Save character
        saveCharacter.addEventListener('click', () => {
            const charName = document.getElementById('charName').value;
            const charWeapon = document.getElementById('charWeapon').value;
            const charBio = document.getElementById('charBio').value;
            
            if (!charName || !selectedClass || !charWeapon) {
                alert('Preencha todos os campos obrigatórios!');
                return;
            }
            
            // In a real app, you would save this to a database
            // Here we'll just show a success message and hide the builder
            alert(`Personagem ${charName} criado com sucesso!`);
            characterBuilder.classList.add('hidden');
            createCharacterBtn.innerHTML = '<i class="fas fa-user-edit mr-2"></i> EDITAR PERSONAGEM';
        });
        
        // Load accepted members from localStorage if available
        if (localStorage.getItem('acceptedMembers')) {
            acceptedMembers = JSON.parse(localStorage.getItem('acceptedMembers'));
            initializeMembers();
        }
        
        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                
                const targetId = this.getAttribute('href');
                if (targetId === '#') return;
                
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    // Close mobile menu if open
                    if (!mobileMenu.classList.contains('hidden')) {
                        mobileMenu.classList.add('hidden');
                    }
                    
                    targetElement.scrollIntoView({
                        behavior: 'smooth'
                    });
                }
            });
        });
    </script>
</body>
</html>